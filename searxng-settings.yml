use_default_settings: true

general:
  instance_name: OpenHands Search

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  bind_address: 0.0.0.0
  port: 8080
  secret_key: local-openhands-search
  limiter: false
  image_proxy: true
